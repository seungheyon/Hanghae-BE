spring:
  profiles:
    include: aws

  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: create
    open-in-view: false

  thymeleaf:
    cache: 'false'
